spring.application.name=MediTrack

# ---- Server (Render injects PORT) ----
server.port=${PORT:8080}

# ============================================
# PostgreSQL (Render Postgres / Supabase)
# Values injected from environment variables
# (Render dashboard or backend/.env).
# ============================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# ---- JPA / Hibernate ----
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# ---- Connection Pool (HikariCP) ----
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# ---- Logging (quiet for prod) ----
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# ---- JSON ----
spring.jackson.serialization.indent-output=false

<<<<<<< HEAD
# ============================================
# JWT
# ============================================
=======
# ---- JWT ----
>>>>>>> 114f56b (Dockerfile add)
meditrack.jwt.secret=${JWT_SECRET:MySecretKeyForMediTrackJWTTokenGenerationAndValidation12345}
meditrack.jwt.expirationMs=${JWT_EXPIRATION_MS:86400000}

# ---- File Upload ----
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ---- Default Admin Account ----
app.admin.email=${ADMIN_EMAIL:admin@meditrack.com}
app.admin.password=${ADMIN_PASSWORD:ChangeMe123!}
app.admin.full-name=${ADMIN_FULL_NAME:System Administrator}
<<<<<<< HEAD
=======

# ---- CORS (comma-separated list of allowed origins, e.g. the Render frontend URL) ----
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
>>>>>>> 114f56b (Dockerfile add)
